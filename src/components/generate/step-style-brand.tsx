"use client";

/**
 * @deprecated Replaced by StepStyle in the simplified generation flow.
 * Kept as a stub to avoid broken imports if anything still references it.
 */
export function StepStyleBrand() {
  return (
    <div className="p-8 text-center text-sm text-muted-foreground">
      This step has been replaced by the new Style picker.
    </div>
  );
}
