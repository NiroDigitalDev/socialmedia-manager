"use client";

/**
 * @deprecated Preview panel removed from the simplified generation flow.
 * Kept as a stub to avoid broken imports if anything still references it.
 */
export function PreviewPanel() {
  return null;
}
