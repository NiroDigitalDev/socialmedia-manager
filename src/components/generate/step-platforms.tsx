"use client";

/**
 * @deprecated This step has been removed from the generation flow.
 * The pipeline is now Instagram-only (no platform selection needed).
 * Kept as a stub to avoid broken imports if anything still references it.
 */
export function StepPlatforms() {
  return (
    <div className="p-8 text-center text-sm text-muted-foreground">
      Platform selection has been removed. The generation flow is now
      Instagram-focused.
    </div>
  );
}
